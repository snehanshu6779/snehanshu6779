name: 🧹 Legacy Profile Update

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Profile 2.0 is maintained by .github/workflows/profile.yml"
